# Security
Active testing requires explicit authorization, exact allowlists, rate/concurrency/time/resource limits and audit logging. Safe mode is default. Destructive actions are denied unless a separate explicit policy permits controlled verification. Secrets are redacted from logs and evidence according to policy.
